# CLAUDE.md

首先完整读取并遵守根目录 `AGENTS.md`。

当前任务由 `project-index.yaml` 和 `docs/TASKS.md` 决定。不要一次实现完整游戏；只处理第一个依赖全部DONE的READY任务。配置表是所有玩法数值和内容的唯一真相源。

开始实现前先输出：

1. 当前任务ID和目标。
2. 明确不做项。
3. 预计修改文件白名单。
4. 验收路径和专项测试。
5. 当前Git工作树基线以及需要保护的已有改动。
6. 可能导致BLOCKED的外部工具门。

完成后按 `AGENTS.md` 保存证据、审查diff、提交当前任务并停止。
